def normal_quicksort(list, left_index, right_index):
    length = len(list)

    if length == 1:
        return list
    if left_index < right_index:
        middle = length // 2 # the index of the pivot
        pivot = list[middle]


def partition(list, left, right):



# quicksort that selects a random pivot
def randomized_quicksort(list):
